doctype html
html
  head
    title Eventos App
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css")
    link(rel="stylesheet", href="/styles.css")
  body
    header
      h1 Eventos App
      nav
        a(href='/') Inicio |
        a(href='/pedidos/nuevo') Nuevo Pedido |
        a(href='/inventario/nuevo') Nuevo Material en inventario |
        a(href='/users/nuevo') Crear nuevo usuario |
        if isAuthenticated
          a(href='/signout') Cerrar sesión
        else
          a(href='/login') Iniciar sesión
    main
      block contenido
